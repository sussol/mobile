/**
 * mSupply Mobile
 * Sustainable Solutions (NZ) Ltd. 2020
 */

const initialState = () => ({});

export const RefundReducer = (state = initialState(), action) => {
  const { type } = action;

  switch (type) {
    default:
      return state;
  }
};
