const initialState = () => ({
  total: 0,
  isScanning: false,
});

// eslint-disable-next-line no-unused-vars
export const VaccineReducer = (_state = initialState(), action) => {
  // eslint-disable-next-line no-unused-vars
  const { type } = action;
};
